<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
android:layout_width="match_parent"
android:layout_height="wrap_content"
android:padding="16dp"
android:background="@color/white">

<!-- Linear Layout for Vertical Alignment -->
<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@drawable/border"
    android:layout_marginTop="10dp"
    android:layout_marginBottom="10dp">



    <!-- Email Text View with Border -->
    <TextView
        android:id="@+id/tvLoggedInUserId"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:layout_marginBottom="8dp"
        android:fontFamily="sans-serif-medium"
        android:lineSpacingExtra="4dp"
        android:clickable="true"
        android:focusable="true"
        android:focusableInTouchMode="true" />


    <!-- hospitalName Text View with Border -->
    <TextView
        android:id="@+id/hospitalNameTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:layout_marginBottom="8dp"
        android:fontFamily="sans-serif"
        android:lineSpacingExtra="4dp"
        android:textStyle="bold"/>


    <!-- confirmedAt Text View with Border -->
    <TextView
        android:id="@+id/confirmedAtTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:layout_marginBottom="8dp"
        android:fontFamily="sans-serif"
        android:lineSpacingExtra="4dp"
        android:textStyle="bold"/>


    <!-- startedAt Text View with Border -->
    <TextView
        android:id="@+id/startedAtTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:layout_marginBottom="8dp"
        android:fontFamily="sans-serif"
        android:lineSpacingExtra="4dp"
        android:textStyle="bold"/>

    <!-- SstoppedAt Text View with Border -->
    <TextView
        android:id="@+id/stoppedAtTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:layout_marginBottom="8dp"
        android:fontFamily="sans-serif"
        android:lineSpacingExtra="4dp"
        android:textStyle="bold"/>

</LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>